{
    "betterSnippets.customSnippets": [
        {
            "name": "useState",
            "body": "const [${1:state}, set${1/(.*)/${1:/capitalize}/}] = useState(${2:initialState})",
            // show above TS import suggestion
            "sortText": "!100",
            // make suggestion icon nice
            "type": "Event",
            "when": {
                "locations": ["lineStart"],
                // shortcut for javascriptreact, typescriptreact
                "languages": ["react"]
            }
        }
    ]
}
