{
    "betterSnippets.customSnippets": [
        {
            "name": "useState",
            "body": "const [${1:state}, set${1/(.*)/${1:/capitalize}/}] = useState(${2:initialState})",
            // show above sortText TS import suggestion
            "sortText": "!100",
            // make suggestino icon nice
            "type": "Event",
            "when": {
                "locations": ["lineStart"],
                "languages": ["react"]
            }
        }
    ]
}
