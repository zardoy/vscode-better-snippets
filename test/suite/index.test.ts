import * as vscode from 'vscode'
import { expect } from 'chai'

import assert from 'assert'

suite('Testingk', () => {
    test('succeeds', () => {
        // vscode.
        expect(true).to.eq(true)
    })
})
